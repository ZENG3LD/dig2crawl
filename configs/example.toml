name = "example"
domains = ["example.com"]
start_urls = ["https://example.com/"]
follow_links = true
fetch_method = "Http"

[rate]
requests_per_second = 2.0
min_delay_ms = 500
concurrent_requests = 1
